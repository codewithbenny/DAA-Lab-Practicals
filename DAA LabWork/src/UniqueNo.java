import java.util.Scanner;

public class UniqueNo {
    public static void uniq(int num){
        String digit=(String.valueOf(num));
        int sum=0;
        for (int i = 0; i < digit.length(); i++) {
            sum=Integer.parseInt(digit[0,])
        }

    }
}
